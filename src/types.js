// @flow

export type ProgramType = {
    uuid: string,
    eventName: string,
    createdAt: string,
    description: string,
    submittedPhotos: boolean,
    submittedReceipts: boolean,
    submittedAttendees: boolean
};